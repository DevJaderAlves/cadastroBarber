generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Agendamento {
  id            Int     @id @default(autoincrement())
  nome          String
  telefone      String
  servico       String
  profissional  String
  horario       String
}
